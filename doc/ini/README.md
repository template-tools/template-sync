## INI merger

a ini file merger


# usage

Put the following merge into your template

```json
{
  "template": {
    "files": [
      {
        "merger": "INI",
        "pattern": "*.ini"
      }]
}
```
