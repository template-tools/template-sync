# YAML merger

a general yaml merger

## usage

Put the following merge into your template

```json
{
  "template": {
    "mergers": [
      {
        "merger": "YAML",
        "pattern": "*.yml"
      }]
}
```

## example
